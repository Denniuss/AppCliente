<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.newmantech.appcliente">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_LOCATION_EXTRA_COMMANDS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".activity.ComfirmaCompraActivity"></activity>
        <activity android:name=".activity.CarritoCompraActivity" />
        <activity
            android:name=".activity.DetalleProducto"
            android:label="@string/title_activity_detalle_producto"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity.ListadoProductosNActivity"
            android:label="@string/title_activity_listado_productos_n"
            android:theme="@style/AppTheme.NoActionBar" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <meta-data
            android:name="com.google.android.gms.wallet.api.enabled"
            android:value="true" />
        <!--
<activity
            android:name=".RegresarAlmacenActivity"
            android:label="@string/title_activity_regresar_almacen"></activity>>
        -->
        <activity android:name=".activity.MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activity.MenuActivity" />

        </activity>

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />
        <uses-library android:name="com.google.android.maps" />

        <activity
            android:name=".activity.DetalleActivity"
            android:label="@string/title_activity_bio"
            android:parentActivityName=".activity.Pre_LoginActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activity.Pre_LoginActivity" />
        </activity>

        <activity
            android:name=".activity.DetalleDireccionActivity"
            android:label="@string/title_activity_bio"
            android:parentActivityName=".activity.Pre_LoginActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activity.Pre_LoginActivity" />
        </activity>
        <activity
            android:name=".activity.LoginActivity"
            android:label="Iniciar Sesión">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activity.MainActivity" />
        </activity>
        <activity
            android:name=".activity.Pre_LoginActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
<activity
            android:name=".AtenderPedidoMapsActivity"
            android:label="@string/title_activity_atender_pedido_maps" />!
        -->
        <activity
            android:name=".activity.MenuActivity"
            android:label="Menu"
            android:theme="@style/AppTheme.NoActionBar" />

        <!--<activity
            android:name=".DireccionActivity"
            android:label="Direcciones"
            android:theme="@style/AppTheme.NoActionBar" />-->

        <activity android:name=".activity.DireccionActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activity.MenuActivity" />
        </activity>

        <!--<activity
            <activity
            android:name=".PerfilActivity"
            android:label="Perfil" />!
        -->
        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />
    </application>

</manifest>